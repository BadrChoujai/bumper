name: Keep usage server warm

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping usage server health check
        run: curl -fsS --max-time 30 https://bumper-usage-server.onrender.com/health
